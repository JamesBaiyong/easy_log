# encoding:utf8
from setuptools import setup

# python setup.py bdist_egg
# python setup.py sdist --formats=gztar

setup(
    name='EasyLog',
    version='0.1',
    packages=[
        'easy_log',
        'easy_log.common'
    ],
)

